import { EventTarget, EventHandler } from '../events';
export declare type CompleteProps<C extends EventTarget> = {
    onComplete?: EventHandler<C>;
};
export default function useComplete<C extends EventTarget>(target: C | undefined, props: CompleteProps<C>): void;
