import { LineCap, LineJoin } from '../../utils/types';
import { EventTarget } from '../events';
import { BaseGeometryProps } from './base';
export declare type LineGeometryProps<C extends EventTarget> = BaseGeometryProps<C> & {
    lineJoin?: LineJoin;
    lineCap?: LineCap;
    showDir?: boolean;
    dirColor?: string;
    dirImg?: HTMLImageElement | HTMLCanvasElement;
    isOutline?: boolean;
    outlineColor?: string;
    borderWeight?: number;
    geodesic?: boolean;
};
export default function useLineGeometry<C extends EventTarget>(target: C | undefined, props: LineGeometryProps<C>): void;
