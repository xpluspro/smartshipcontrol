import { EventTarget, EventHandler } from '../events';
export declare type ClickProps<C extends EventTarget> = {
    onClick?: EventHandler<C>;
};
export default function useClick<C extends EventTarget>(target: C | undefined, props: ClickProps<C>): void;
