import { StrokeStyle } from '../../utils/types';
import { OverlayProps } from '../common/overlay';
import { EventTarget } from '../events';
export declare type BaseGeometryProps<C extends EventTarget> = OverlayProps<C> & {
    strokeColor?: string;
    strokeOpacity?: number;
    strokeWeight?: number;
    strokeStyle?: StrokeStyle;
    strokeDasharray?: number[];
};
export default function useBaseGeometry<C extends EventTarget>(target: C | undefined, props: BaseGeometryProps<C>): void;
